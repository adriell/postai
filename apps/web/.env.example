# URL pública da API — usada pelo browser (client components)
NEXT_PUBLIC_API_URL=https://sua-api.railway.app

# URL interna da API — usada pelo Next.js server (rewrites)
# Pode ser igual a NEXT_PUBLIC_API_URL
INTERNAL_API_URL=https://sua-api.railway.app
